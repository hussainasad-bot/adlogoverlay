org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
kotlin.code.style=official
android.useAndroidX=true
android.nonTransitiveRClass=true

# ---- Release ------------------------------------------------------------------------------
# The version published. `./gradlew publishNewVersion -Pbump=patch` raises it and saves it here.
VERSION_NAME=1.0.0

# <owner>/<repo> on GitHub. The package is published to this repository's GitHub Packages, and it
# becomes the project and source links in the POM. If the repository moves, change it here.
GITHUB_REPO=hussainasad-bot/adlogoverlay
